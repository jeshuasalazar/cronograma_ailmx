{
  "name": "cronograma-ailmx",
  "version": "1.0.0",
  "private": true,
  "description": "Panel de operaciones aiLearning para despliegue en Railway.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist --single -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
